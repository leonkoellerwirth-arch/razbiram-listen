{
  "name": "razbiram-listen-viewer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Karaoke reading viewer for razbiram-listen .listen.json files — local, no upload.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vite": "^8.1.4",
    "vitest": "^3.0.0"
  }
}
